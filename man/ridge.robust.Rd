% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ridge.robust.R
\name{ridge.robust}
\alias{ridge.robust}
\title{Internal lassoenet function}
\usage{
ridge.robust(x = x, y = y, type.lambda = type.lambda, al = 0,
  gamma.seq = gamma.seq, err.curves = err.curves,
  result.matrix = result.matrix, CI = CI, B.rep = B.rep,
  significance = significance, dataa = dataa, xx.indices = xx.indices,
  parallel = parallel)
}
\arguments{
\item{x}{A \code{model.matrix} of the predictors.\cr}

\item{y}{A vector of response values for the model fitting.\cr}

\item{type.lambda}{Either "lambda.min" or "lambda.1se".\cr}

\item{al}{0 for ridge.\cr}

\item{gamma.seq}{The gamma sequence to try.\cr}

\item{err.curves}{The number of error curves to be fitted. Default is 0.\cr}

\item{result.matrix}{A matrix for storing the results.\cr}

\item{CI}{TRUE for residual bootstrapping. In this version is always TRUE.}

\item{B.rep}{The number of residual bootstrappings to do.}

\item{significance}{The significance level of the confidence intervals e.g. 100(1-\eqn{\alpha})\%.\cr}

\item{dataa}{Your full \code{data.frame}}

\item{xx.indices}{Locations of the predictors within dataa.}

\item{parallel}{Parallelisation}
}
\value{
A vector of outputs of the best Adpative Lasso model and the 100(1-\eqn{\alpha})\% confidence intervals for the point estimates from using residual bootstrapping.
}
\description{
Internal lassoenet function
}
\section{Details}{
 These are not intended for use by users. This function is one of the main engines for the Adaptive Lasso computation. This function is used when the weighting method "ridge" is selected.
This together with the function \code{\link{ridge.approach}} forms the computation operator for the Adaptive Lasso when using the weighting method "ridge". The return from this function will enter \code{\link{Adlasso}} for futher wrapping.
}

\author{
Mokyo Zhou
}
